{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
{% set fighterLength = 0 %}
        <div class="row">
        <div class="col-md-6">
            {% for ageGroupName, ageGroup in ageGroupFighters %}
                <div class="page-header">
                    <h2>{{ ageGroupName }}</h2>
                    {{ ageGroup.fighters|length }} Kämpfer
                </div>
                <table class="table table-striped">
                  <thead>
                    <tr>
                      <th>#</th>
                      <th>{% trans %}Last name{% endtrans %}</th>
                      <th>{% trans %}First name{% endtrans %}</th>
                      <th>{% trans %}Weight {% endtrans %}</th>
                      <th>{% trans %}Age group {% endtrans %}</th>
                      <th>{% trans %}Group id {% endtrans %}</th>
                      <th>{% trans %}Club {% endtrans %}</th>
                      <th>&nbsp;</th>
                    </tr>
                  </thead>
                      <tbody>
                          {% for fighter in ageGroup.fighters %}
                          <tr id="dragFighter">
                              <td style="display:none" id="fighterId">{{ fighter.id }}</td>
                              <td >{{ loop.index }}</td>
                              <td >{{fighter.lastName}}</td>
                              <td>{{fighter.firstName}}</td>
                              <td>{{fighter.weight}}</td>
                              <td>{{fighter.ageGroup}}</td>
                              <td>{{fighter.groupId}}</td>
                              <td>{{fighter.club}}</td>
                              <td>
                                  <a href="{{ url('editFighter', {'id': fighter.id}) }}" >
                                      <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
                                  </a>
                              </td>
                          </tr>
                          {% endfor %}
                      </tbody>
                </table>
                {% endfor %}
        </div>
     </div>
    </div>



{% endblock %}
